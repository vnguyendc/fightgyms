# Placeholders only. Export in your shell; scripts do not auto-load .env.
# Standalone Jev triage needs only TYPESAFE_API_KEY. Never commit a real key.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-1.13.0

# Existing scraper only; not needed for saved-corpus triage.
ANTHROPIC_API_KEY=
DATABASE_URL=
